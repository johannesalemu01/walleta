# Copy this file to android/keystore.properties after running: npx expo prebuild --platform android
# Fill in the values. Do NOT commit android/keystore.properties or the .keystore file.
# storeFile is relative to android/app/ (e.g. ../release.keystore if keystore is in android/)
storeFile=../release.keystore
storePassword=YOUR_STORE_PASSWORD
keyAlias=birrtrack
keyPassword=YOUR_KEY_PASSWORD
